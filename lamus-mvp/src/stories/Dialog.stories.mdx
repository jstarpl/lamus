import { Meta, Story } from "@storybook/addon-docs";
import { Dialog } from "../components/Dialog";

<!--👇 The title prop determines where your story goes in the story list -->

<Meta title="Dialog" component={Dialog} />

<!--👇 We create a “template” of how args map to rendering -->

export const Template = (args) => <Dialog {...args} />;

<!-- 👇 The args you need here will depend on your component -->

<Story
  name="With single accept Button"
  args={{
    children: (
      <>
        <h2>Title goes here</h2>
        <p>Message body goes here</p>
        <div className="buttons">
          <button tabIndex={0} data-accept type="submit">
            OK
          </button>
        </div>
      </>
    ),
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="With Text Input"
  args={{
    children: (
      <>
        <p>Message body goes here</p>
        <p>
          <input
            tabIndex={1}
            type="text"
            className="form-control"
            placeholder="A prompt..."
          />
        </p>
        <div className="buttons">
          <button tabIndex={0} data-accept type="submit">
            OK
          </button>
        </div>
      </>
    ),
  }}
>
  {Template.bind({})}
</Story>
